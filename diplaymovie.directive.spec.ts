import { DiplaymovieDirective } from './diplaymovie.directive';

describe('DiplaymovieDirective', () => {
  it('should create an instance', () => {
    const directive = new DiplaymovieDirective();
    expect(directive).toBeTruthy();
  });
});
