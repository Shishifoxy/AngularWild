import { ColorOnlyOneLiDirective } from './color-only-one-li.directive';

describe('ColorOnlyOneLiDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorOnlyOneLiDirective();
    expect(directive).toBeTruthy();
  });
});
